double current = 1; // The number that we are currently dividing by, don't touch it
double pi = 0; // Pi, don't touch it

// Acutal configuration
int precision = 22; // The precision of the float that will be sent to the console. max 22
float maxtries = 10E2; // The maximum attempts to find pi